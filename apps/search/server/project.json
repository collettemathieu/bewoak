{
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "name": "apps-search-server",
    "projectType": "application",
    "sourceRoot": "apps/search/server/src",
    "tags": ["apps-search-server"],
    "targets": {
        "serve": {
            "configurations": {
                "development": {
                    "buildTarget": "apps-search-server:build:development"
                },
                "production": {
                    "buildTarget": "apps-search-server:build:production"
                }
            },
            "defaultConfiguration": "development",
            "executor": "@nx/js:node",
            "options": {
                "buildTarget": "apps-search-server:build"
            }
        },
        "test-feature": {
            "executor": "nx:run-commands",
            "options": {
                "command": "TS_NODE_PROJECT='{projectRoot}/tsconfig.spec.json' bun run cucumber-js --config={projectRoot}/cucumber.js"
            }
        }
    }
}
